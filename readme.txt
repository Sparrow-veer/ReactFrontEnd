Screenshot of working application is added in both Reactfrontend and nodebackend repositories


Script to install dependencies:

npm install
to add dependencies for node backend application

yarn install 
to add dependencies for react application



Starting script:

backend application is listening at 4000

execute below command inside node applicatin
node index.js

Backend application is listening at 3000

execute below command inside react application
npm start



Swagger endpoint:

http://localhost:4000/api-docs/

screenshots of working swagger endpoints is added in both Reactfrontend and nodebackend repositories



